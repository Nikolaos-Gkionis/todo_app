# New Fixes (March 3)

- [ ] Create a new page path should be a modal, not a full page
- [ ] remove the week view from that path, rename pages to lists
- [ ] remove view from the bottom menu and add the missing views in the preferences
- [ ] remove the serch icon and functionality 
- [ ] move the fab back to the bottom left corner
- [ ] the calendar modal is not working, once open the user needs the ability to jump on the selected date.
- [ ] move the darker border of a highlighted item to the right or add some padding so it does not touch the text
- [ ] recurrance needs to populate the relevant fields as soon as the user selects it. For example when something is repeated evryday the following days need to replicate that to-do across the calendar view
