# Moved

This helper now lives next to the peponi.to app repo:

**`/home/nikolaos/Documents/GitHub/peponi-omarchy`**

Install (once published to GitHub):

```bash
omarchy plugin add https://github.com/Nikolaos-Gkionis/peponi-omarchy.git --enable
~/.config/omarchy/plugins/peponi.one-day/scripts/setup.sh
```

Local / current machine:

```bash
cd ~/Documents/GitHub/peponi-omarchy
./scripts/install.sh
./scripts/setup.sh
```

Rails `/api/v1` desktop endpoints remain in this `todo_app` repository.
