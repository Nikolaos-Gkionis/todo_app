<!-- Responsive Navigation Bar -->
<nav class="bg-white shadow-sm">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center py-6">
      <!-- Logo -->
      <div class="flex items-center">
        <%= link_to (defined?(root_path) ? root_path : pages_path), class: "flex items-center" do %>
          <div class="logo-checkbox mr-3"></div>
          <span class="text-2xl font-bold text-gray-900">Todo-it</span>
        <% end %>
      </div>
      
      <!-- Desktop Navigation - Always visible on desktop -->
      <div class="hidden sm:flex items-center space-x-8">
        <% if @marketing_nav %>
          <!-- Marketing Navigation -->
          <%= link_to "Home", root_path, class: "#{current_page?(root_path) ? 'text-blue-600' : 'text-gray-600 hover:text-blue-600'} font-medium" %>
          <%= link_to "Pricing", pricing_path, class: "#{current_page?(pricing_path) ? 'text-blue-600' : 'text-gray-600 hover:text-blue-600'} font-medium" %>
          <%= link_to "How to Use", how_to_path, class: "#{current_page?(how_to_path) ? 'text-blue-600' : 'text-gray-600 hover:text-blue-600'} font-medium" %>
          <div class="flex items-center space-x-4">
            <%= link_to "Sign In", login_path, class: "text-gray-600 hover:text-blue-600 font-medium" %>
            <%= link_to "Get Started", signup_path, class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg font-medium transition-colors" %>
          </div>
        <% elsif logged_in? %>
          <!-- Authenticated User Navigation -->
          <span class="text-gray-700"><%= random_motivational_message %>, <%= current_user.display_name %>!</span>
          <%= link_to settings_path, class: "text-gray-600 hover:text-gray-800 font-medium text-sm" do %>
            <svg class="w-5 h-5 inline-block" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
            </svg>
          <% end %>
          <%= link_to "Logout", logout_path, data: { "turbo-method": :delete },
                class: "text-red-600 hover:text-red-800 font-medium hand-underline hand-underline-red text-sm" %>
        <% end %>
      </div>
      
      <!-- Mobile/Tablet Menu Button - Only visible on smaller screens -->
      <div class="block sm:hidden">
        <button type="button" class="mobile-menu-button text-gray-600 hover:text-gray-800 focus:outline-none focus:text-gray-800" aria-controls="mobile-menu" aria-expanded="false">
          <span class="sr-only">Open main menu</span>
          <!-- Hamburger Icon -->
          <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
  </div>
  
    <!-- Mobile/Tablet Full-Screen Menu (Hidden by default) -->
    <div class="mobile-menu hidden sm:hidden fixed inset-0 z-50 bg-white" id="mobile-menu">
    <div class="flex flex-col h-full">
      <!-- Mobile Menu Header -->
      <div class="flex justify-between items-center py-6 px-4 border-b border-gray-200">
        <%= link_to (defined?(root_path) ? root_path : pages_path), class: "flex items-center" do %>
          <div class="logo-checkbox mr-3"></div>
          <span class="text-2xl font-bold text-gray-900">Todo-it</span>
        <% end %>
        <button type="button" class="mobile-menu-close text-gray-600 hover:text-gray-800 focus:outline-none">
          <span class="sr-only">Close menu</span>
          <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
      
      <!-- Mobile Menu Content -->
      <div class="flex-1 px-4 py-6 space-y-8">
        <% if @marketing_nav %>
          <!-- Marketing Mobile Navigation -->
          <%= link_to "Home", root_path, class: "block text-3xl font-medium #{current_page?(root_path) ? 'text-blue-600' : 'text-gray-900 hover:text-blue-600'}" %>
          <%= link_to "Pricing", pricing_path, class: "block text-3xl font-medium #{current_page?(pricing_path) ? 'text-blue-600' : 'text-gray-900 hover:text-blue-600'}" %>
          <%= link_to "How to Use", how_to_path, class: "block text-3xl font-medium #{current_page?(how_to_path) ? 'text-blue-600' : 'text-gray-900 hover:text-blue-600'}" %>
          <div class="pt-8 space-y-6">
            <%= link_to "Sign In", login_path, class: "block text-2xl font-medium text-gray-600 hover:text-blue-600" %>
            <%= link_to "Get Started", signup_path, class: "block bg-blue-600 hover:bg-blue-700 text-white px-8 py-4 rounded-lg text-2xl font-medium text-center transition-colors" %>
          </div>
        <% elsif logged_in? %>
          <!-- Authenticated User Mobile Navigation -->
          <div class="text-xl text-gray-700 mb-8">
            <%= random_motivational_message %>, <%= current_user.display_name %>!
          </div>
          <%= link_to settings_path, class: "flex items-center text-3xl font-medium text-gray-900 hover:text-blue-600" do %>
            <svg class="w-8 h-8 mr-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
            </svg>
            Settings
          <% end %>
          <%= link_to "Logout", logout_path, data: { "turbo-method": :delete },
                class: "block text-3xl font-medium text-red-600 hover:text-red-800 hand-underline hand-underline-red" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>

<!-- Mobile Menu JavaScript -->
<script>
// Function to initialize mobile menu
function initializeMobileMenu() {
  const mobileMenuButton = document.querySelector('.mobile-menu-button');
  const mobileMenu = document.querySelector('.mobile-menu');
  const mobileMenuClose = document.querySelector('.mobile-menu-close');
  
  if (mobileMenuButton && mobileMenu && mobileMenuClose) {
    // Remove existing listeners to prevent duplicates
    mobileMenuButton.removeEventListener('click', handleMobileMenuToggle);
    mobileMenuClose.removeEventListener('click', handleMobileMenuClose);
    
    // Add event listeners
    mobileMenuButton.addEventListener('click', handleMobileMenuToggle);
    mobileMenuClose.addEventListener('click', handleMobileMenuClose);
  }
}

// Handle mobile menu toggle
function handleMobileMenuToggle(e) {
  e.preventDefault();
  const mobileMenu = document.querySelector('.mobile-menu');
  const mobileMenuButton = document.querySelector('.mobile-menu-button');
  
  if (mobileMenu && mobileMenuButton) {
    const isHidden = mobileMenu.classList.contains('hidden');
    if (isHidden) {
      mobileMenu.classList.remove('hidden');
      mobileMenuButton.setAttribute('aria-expanded', 'true');
      document.body.style.overflow = 'hidden';
    } else {
      mobileMenu.classList.add('hidden');
      mobileMenuButton.setAttribute('aria-expanded', 'false');
      document.body.style.overflow = '';
    }
  }
}

// Handle mobile menu close
function handleMobileMenuClose(e) {
  e.preventDefault();
  const mobileMenu = document.querySelector('.mobile-menu');
  const mobileMenuButton = document.querySelector('.mobile-menu-button');
  
  if (mobileMenu && mobileMenuButton) {
    mobileMenu.classList.add('hidden');
    mobileMenuButton.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  }
}

// Close mobile menu when clicking outside (using event delegation)
document.addEventListener('click', function(e) {
  const mobileMenuButton = document.querySelector('.mobile-menu-button');
  const mobileMenu = document.querySelector('.mobile-menu');
  
  if (mobileMenuButton && mobileMenu && 
      !mobileMenuButton.contains(e.target) && 
      !mobileMenu.contains(e.target) && 
      !mobileMenu.classList.contains('hidden')) {
    mobileMenu.classList.add('hidden');
    mobileMenuButton.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  }
});

// Close mobile menu on escape key
document.addEventListener('keydown', function(e) {
  if (e.key === 'Escape') {
    const mobileMenu = document.querySelector('.mobile-menu');
    const mobileMenuButton = document.querySelector('.mobile-menu-button');
    
    if (mobileMenu && mobileMenuButton && !mobileMenu.classList.contains('hidden')) {
      mobileMenu.classList.add('hidden');
      mobileMenuButton.setAttribute('aria-expanded', 'false');
      document.body.style.overflow = '';
    }
  }
});

// Initialize on DOM ready and after Turbo navigation
document.addEventListener('DOMContentLoaded', initializeMobileMenu);
document.addEventListener('turbo:load', initializeMobileMenu);
document.addEventListener('turbo:render', initializeMobileMenu);
</script>
